name: Build

on: [push]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.1
      
    - name: Build Debug x64
      run: msbuild HLS_Multihack.sln /p:Configuration=Debug /p:Platform=x64
